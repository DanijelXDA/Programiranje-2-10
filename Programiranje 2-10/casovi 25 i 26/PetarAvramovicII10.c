#include <stdio.h>
#define MAX 50

int main(void)
{
    int a[MAX], n ,p;
    do{
        printf("\nUnesite element u nizu: ");
        scanf("%d", &n);
    }
    for(i=n-1;i>0;i--)
        a[i]=a[i-1]
        a[0]=p;

    return 0;

}
